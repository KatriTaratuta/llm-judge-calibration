# llm-judge-calibration

Minimal tools for inter-rater reliability and judge calibration in LLM evaluation pipelines.

## Features

- Calculate inter-rater agreement (Cohen's kappa, Fleiss' kappa)
- Monitor judge drift over time
- Utilities for processing evaluation outputs

## Installation

```bash
pip install -r requirements.txt
